name: Monthly 311 data refresh

on:
  schedule:
    - cron: '0 6 1 * *'
  workflow_dispatch:

jobs:
  refresh:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          lfs: true

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install -r backend/requirements.txt

      - name: Run ingestion
        working-directory: backend/notebooks
        run: |
          jupyter nbconvert \
            --to notebook --execute --inplace \
            --ExecutePreprocessor.timeout=3600 \
            00_ingest.ipynb

      - name: Run feature engineering
        working-directory: backend/notebooks
        run: |
          jupyter nbconvert \
            --to notebook --execute --inplace \
            --ExecutePreprocessor.timeout=3600 \
            03_feature_engineering.ipynb

      - name: Check data files exist
        working-directory: backend
        run: |
          echo "=== data/processed contents ==="
          ls -lh data/processed/
          echo "=== checking parquet row count ==="
          python -c "
          import pandas as pd
          import os
          for f in os.listdir('data/processed'):
              if f.endswith('.parquet') or f.endswith('.csv'):
                  path = f'data/processed/{f}'
                  try:
                      df = pd.read_parquet(path) if f.endswith('.parquet') else pd.read_csv(path)
                      print(f'{f}: {len(df):,} rows')
                  except Exception as e:
                      print(f'{f}: ERROR - {e}')
          "

      - name: Run model training
        working-directory: backend/notebooks
        run: |
          jupyter nbconvert \
            --to notebook --execute --inplace \
            --ExecutePreprocessor.timeout=3600 \
            04_training_models.ipynb

      - name: Commit updated files to GitHub
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add backend/data/processed/
          git add backend/models/
          git diff --staged --quiet || git commit -m "auto: 311 refresh $(date +%Y-%m-%d)"
          git push

      - name: Push updated artifacts to HuggingFace Space
        run: |
          python - <<'EOF'
          from huggingface_hub import HfApi
          import os

          api = HfApi()
          token = os.environ["HF_TOKEN"]
          repo_id = "idakam/nyc-noise-backend"

          files_to_push = [
              "backend/data/processed/noise_cleaned.parquet",
              "backend/data/processed/aggregated_data.parquet",
              "backend/data/processed/last_ingested.txt",
              "backend/data/processed/type_distribution.json",
              "backend/data/processed/accuracy_log.json",
              "backend/data/processed/pipeline_runs.json",
              "backend/models/complaint_volume_model.pkl",
              "backend/models/model_metadata.pkl",
              "backend/models/feature_columns.pkl",
          ]

          for path in files_to_push:
              if os.path.exists(path):
                  repo_path = path.replace("backend/", "", 1)
                  api.upload_file(
                      path_or_fileobj=path,
                      path_in_repo=repo_path,
                      repo_id=repo_id,
                      repo_type="space",
                      token=token,
                  )
                  print(f"✓ {path} → {repo_path}")
              else:
                  print(f"  skipped (not found): {path}")
          EOF
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}